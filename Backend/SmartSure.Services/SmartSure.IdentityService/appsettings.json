{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "AuthDb": "${DB_CONNECTION_STRING}"
  },
  "Jwt": {
    "Key": "${JWT_SECRET}",
    "Issuer": "${JWT_ISSUER}",
    "Aud1": "${JWT_AUDIENCE1}",
    "Aud2": "${JWT_AUDIENCE2}",
    "Aud3": "${JWT_AUDIENCE3}",
    "Aud4": "${JWT_AUDIENCE4}",
    "Aud5": "${JWT_AUDIENCE5}"
  },
  "AdminSettings": {
    "Password": "${ADMIN_PASSWORD}"
  },
  "Google": {
    "ClientId": "${GOOGLE_CLIENT_ID}",
    "ClientSecret": "${GOOGLE_CLIENT_SECRET}",
    "RedirectUri": "${GOOGLE_REDIRECT_URI}",
    "FrontendCallbackUrl": "${GOOGLE_FRONTEND_CALLBACK_URL}"
  },
  "Email": {
    "SmtpHost": "${SMTP_HOST}",
    "SmtpPort": "${SMTP_PORT}",
    "SmtpUser": "${SMTP_USER}",
    "SmtpPass": "${SMTP_PASS}",
    "FromEmail": "${FROM_EMAIL}",
    "FromName": "${FROM_NAME}"
  },
  "RabbitMq": {
    "HostName": "${RABBITMQ_HOST}",
    "Port": "${RABBITMQ_PORT}",
    "UserName": "${RABBITMQ_USER}",
    "Password": "${RABBITMQ_PASS}",
    "VirtualHost": "${RABBITMQ_VHOST}",
    "ExchangeName": "${RABBITMQ_EXCHANGE}"
  },
  "AllowedHosts": "*"
}
