# SmartSure Frontend

The Angular 17 frontend now lives in [smartsure-ui](smartsure-ui). The old static starter files in this folder are legacy and can be ignored.

## Run the Angular app

```bash
cd smartsure-ui
npm install
npm start
```

The app talks to the gateway at `http://localhost:5000/gateway`.
